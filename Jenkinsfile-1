pipeline {
  agent {label 'linux'}

  stages {
    stage('Hello') {
      echo "ANSIBLE_GROUP ${params.ANSIBLE_GROUP}"
      steps {
        echo "ANSIBLE_GROUP ${params.ANSIBLE_GROUP}"
        sh '''
          ansible --version
          ansible-playbook --version
          ansible-galaxy --version
        '''
      }
    }
  }
}
